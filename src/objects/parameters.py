class Parameters:
    def __init__(self, player_speed, crewmate_vision, impostor_vision, impaired_vision, vote_time, kill_cooldown, kill_distance, common_tasks, long_tasks, short_tasks):
        self.player_speed = player_speed
        self.crewmate_vision = crewmate_vision
        self.impostor_vision = impostor_vision
        self.impaired_vision = impaired_vision
        self.vote_time = vote_time
        self.kill_cooldown = kill_cooldown
        self.kill_distance = kill_distance
        # self.common_taksk = common_tasks
        # self.long_tasks = long_tasks
        # self.short_tasks = short_tasks
